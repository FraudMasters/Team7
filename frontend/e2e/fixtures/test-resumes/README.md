# Test Resume Fixtures

This directory contains test resume files for end-to-end testing.

## Test Files

The test suite will dynamically create minimal PDF files for testing purposes.
These files are only used for integration testing and are not actual resumes.

## Purpose

These fixture files are used by:
- `bulk-operations.spec.ts` - Tests for ZIP file upload functionality
- Any other E2E tests that require resume files

## Note

These are synthetic test files, not real candidate data.
